n=input('Enter a number:');
fib[0,1];
while true
  fib_next = fib(end) + fib(end-1);
  if fib_next > n 
      break;
  end 
  fib=[fib, fib_next]; 
end 
fprintf('Fibonacci series up to %d: ', n);
fprintf('%d', fib);
fprintf('\n'); 
